f([], 0).
f([H|T], S) :- f(T, S1), S1 >= 2, !, S = S1 + H.
f([_|T], S) :- f(T, S1), S = S1+1.

g([], 0).
g([H|T], S) :- g(T, S1), h(S1, H, S).

h(S1, H, S) :- S1 >= 2, !, S = S1 + H. 
h(S1, _, S) :- S = S1+1.
